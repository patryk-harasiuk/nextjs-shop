import type { NextPage } from 'next';

import Layout from '../components/layout/Layout';

const Home: NextPage = () => {
  return (
    <Layout>
      <div className="min-h-screen">
        <h2>MAIN PAGE</h2>
      </div>
    </Layout>
  );
};

export default Home;
